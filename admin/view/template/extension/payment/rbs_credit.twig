{{ header }}{{ column_left }}

<div id="content">
    <div class="page-header">
        <div class="container-fluid">
            <div class="pull-right">
                <button type="submit" form="form-rbs" data-toggle="tooltip" title="{{ rbs_credit_button_save }}" class="btn btn-primary"><i class="fa fa-save"></i></button>
                <a href="{{ cancel }}" data-toggle="tooltip" title="{{ rbs_credit_button_cancel }}" class="btn btn-default"><i class="fa fa-reply"></i></a>
            </div>
            <h1>{{ heading_title }}</h1>
            <ul class="breadcrumb">
                {% for breadcrumb in breadcrumbs %}
                    <li><a href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a></li>
                {% endfor %}
            </ul>
        </div>
    </div>

    <div class="container-fluid">

        <div class="panel panel-default">
            <div class="panel-heading">
                <h3 class="panel-title"><i class="fa fa-pencil"></i> <?php echo $text_settings; ?></h3>
            </div>
            <div class="panel-body">
                <form action="{{ action }}" method="post" enctype="multipart/form-data" id="form-rbsCredit" class="form-horizontal">

                    <!-- Статус: Включен/Выключен -->
                    <div class="form-group">
                        <label class="col-sm-3 control-label">
                            {{ entry_status }}
                        </label>
                        <div class="col-sm-9">
                            <select name="payment_rbs_credit_status" class="form-control">
                                {% if payment_rbs_credit_status %}
                                    <option value="1" selected="selected">{{ status_enabled }}</option>
                                    <option value="0">{{ status_disabled }}</option>
                                {% else %}
                                    <option value="1">{{ status_enabled }}</option>
                                    <option value="0" selected="selected">{{ status_disabled }}</option>
                                {% endif %}
                            </select>
                        </div>
                    </div>

                    <!-- Логин продавца -->
                    <div class="form-group">
                        <label class="col-sm-3 control-label" for="">
                            {{ entry_merchant_login }}
                        </label>
                        <div class="col-sm-9">
                            <input type="text" name="payment_rbs_credit_merchant_login" value="{{ payment_rbs_credit_merchant_login }}" class="form-control" />
                        </div>
                    </div>

                    <!-- Пароль продавца -->
                    <div class="form-group">
                        <label class="col-sm-3 control-label" for="">
                            {{ entry_merchant_password }}
                        </label>
                        <div class="col-sm-9">
                            <input type="password" name="payment_rbs_credit_merchant_password" value="{{ payment_rbs_credit_merchant_password }}" class="form-control" />
                        </div>
                    </div>

                    <!-- Режим работы модуля: Тестовый/БоевойРежим работы модуля: Тестовый/Боевой -->
                    <div class="form-group">
                        <label class="col-sm-3 control-label">
                            {{ entry_mode }}
                        </label>
                        <div class="col-sm-9">
                            <select name="payment_rbs_credit_mode" class="form-control">
                                {% if payment_rbs_credit_mode == "test" %}
                                    <option value="test" selected="selected">{{ rbs_credit_mode_test }}</option>
                                    <option value="prod">{{ rbs_credit_mode_prod }}</option>
                                {% else %}
                                    <option value="test">{{ rbs_credit_mode_test }}</option>
                                    <option value="prod" selected="selected">{{ rbs_credit_mode_prod }}</option>
                                {% endif %}
                            </select>
                        </div>
                    </div>

                    <!-- Urls -->
                    <div class="form-group">
                        <label class="col-sm-3 control-label" for="">
                            {{ entry_rbs_credit_returnUrl }}
                        </label>
                        <div class="col-sm-9">
                            <input type="text" name="payment_rbs_credit_returnUrl" value="{{ payment_rbs_credit_returnUrl }}" class="form-control" />
                            Страница, на которую будет перенаправлен пользователь в случае успешной оплаты.<br />
                            Оставьте это поле пустым, если хотите использовать настройки по умолчанию.
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="col-sm-3 control-label" for="">
                            {{ entry_rbs_credit_failUrl }}
                        </label>
                        <div class="col-sm-9">
                            <input type="text" name="payment_rbs_credit_failUrl" value="{{ payment_rbs_credit_failUrl }}" class="form-control" />
                            Страница, на которую будет перенаправлен пользователь в случае неудачной оплаты.<br />
                            Оставьте это поле пустым, если хотите использовать настройки по умолчанию.
                        </div>
                    </div>


                    <div class="form-group">
                        <label class="col-sm-3 control-label" for="">
                            Порядок сортировки
                        </label>
                        <div class="col-sm-9">
                            <input type="text" name="payment_rbs_credit_sort_order" value="{{ payment_rbs_credit_sort_order }}" class="form-control" />
                        </div>
                    </div>

                    <!-- Логирование: Включено/Выключено -->
                    <div class="form-group">
                        <label class="col-sm-3 control-label">
                            {{ entry_logging }}
                        </label>
                        <div class="col-sm-9">
                            <select name="payment_rbs_credit_logging" class="form-control">
                                <option value="1" {{ payment_rbs_credit_logging  == 1 ? 'selected="selected"' : '' }}>{{ rbs_credit_logging_enabled }}</option>
                                <option value="0" {{ payment_rbs_credit_logging  == 0 ? 'selected="selected"' : '' }}>{{ rbs_credit_logging_disabled }}</option>
                            </select>
                        </div>
                    </div>

                </form>
            </div>
        </div>
    </div>
</div>
<?php echo $footer; ?>